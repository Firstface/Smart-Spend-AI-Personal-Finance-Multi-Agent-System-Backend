[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: Pure unit tests, no external dependencies
    integration: Tests requiring LLM/DB connections
addopts = -v --tb=short --continue-on-collection-errors
